name: C/C++ CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: show files
      run: ls
    - name: dir
      run: mkdir build && cd build
    - name: show files
      run: ls
    - name: cmake
      run: pwd && ls && cmake
    - name: make
      run: cd build && make
